# Production (e.g. Render static site): full origin of the API, no trailing slash.
# Example: https://hypoguard-api.onrender.com
# Leave unset for local dev (Vite proxies /api → port 8000).
# VITE_API_BASE_URL=
